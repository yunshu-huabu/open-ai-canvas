AGENTS.git.md

本文档用于约束本项目中的 AI / 自动化开发行为。开发时优先遵循本文件，其次遵循用户当前消息。

## 一、Git 工作流（核心约束）

### 1.1 提交规范

- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
- Commit message 需清晰描述本次改动内容，推荐使用 Conventional Commits 前缀：
  - `feat:` 新增功能
  - `fix:` 修复 Bug
  - `chore:` 构建/工具/依赖变更
  - `refactor:` 代码重构（不改变功能）
  - `docs:` 文档更新
  - `test:` 测试相关

### 1.2 测试与验证

- 每次改动后，都必须编写或更新相关测试。
- 在交付给用户前，必须确保所有测试和验证全部通过。
- 严禁在测试未通过的情况下提交代码**。**

### 1.3 推送同步

- 只有测试和验证全部通过后，用户确认才允许执行 `git commit`。
- 验证通过并 commit 后，必须推送到 GitHub，确保远程仓库同步：
  ```bash
  git push origin main
  ```
